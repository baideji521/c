# FINAL_REPORT

> 本报告所有数值均来自 `reports/results.json` 的实际运行结果，未做任何估算。

## 运行环境

- os: Windows 10
- python: 3.11.9
- cpu: Intel64 Family 6 Model 79 Stepping 1, GenuineIntel
- torch: 2.6.0+cu124
- cuda_available: True
- gpu: NVIDIA GeForce RTX 3060
- vram_mb: 12287
- cuda_version: 12.4
- ffmpeg: ffmpeg version 9.0.1-essentials_build-www.gyan.dev Copyright (c) 2000-2026 the FFmpeg developers
- model: isc21

## 推荐算法组合：**D**

- 覆盖用例：basic, distractor, multi, robust, unknown
- 累计评测 GT 片段数：25

## 二十问

**1. 是否能够从一个成片中找回原始素材？**
能。推荐组合在全部用例上的 source_recall = 1.0000，segment_recall(IoU>=0.5) = 1.0000。

**2. 支持多少个 source？**
架构上不限（逐素材独立建索引，绝不拼接时间轴）。本次实测单用例最多 5 个素材。

**3. 是否支持一个成片来自多个 source？**
支持。multi 用例的成片由 5 个素材、6 个片段混剪而成，逐片段独立判定来源，本次 source_recall=1.0000。

**4. 是否支持同一个 source 被重复使用？**
支持。multi 用例含「重复使用 Source01」的 GT 片段，本次判定：1/1 正确，起点平均误差 0.015 s

**5. 是否支持裁剪？** 1/1 正确，起点平均误差 0.000 s

**6. 是否支持缩放？** 1/1 正确，起点平均误差 0.020 s

**7. 是否支持重编码？** 1/1 正确，起点平均误差 0.040 s

**8. 是否支持字幕？** 1/1 正确，起点平均误差 0.476 s

**9. 是否支持水印？** 1/1 正确，起点平均误差 0.131 s

**10. 是否支持轻微变速？** 2/2 正确，起点平均误差 0.076 s

**附：画面裁切（10%）** 1/1 正确，起点平均误差 0.120 s

**11. 二次加工以后准确率多少？**
robust 用例（含两次加工组合）：source_recall=1.0000，segment_recall=1.0000。其中「两次加工」项：1/1 正确，起点平均误差 0.264 s

**12. 平均开始时间误差多少？** 0.0390 秒

**13. 平均结束时间误差多少？** 0.0726 秒

**14. Temporal IoU 多少？** 原素材时间轴 IoU 中位数 = 0.9778

**15. 误匹配率多少？** false_positive_rate = 0.0000；应判 UNKNOWN 的片段判定正确率 = 1.0000

**16. RTX 3060 12GB 是否可以运行？** 可以。设备=NVIDIA GeForce RTX 3060，本次峰值显存 788.4 MB，远低于 12GB。CUDA 不可用时自动回退 CPU。

**17. 第一次运行耗时多少？** 14.06 秒（用例 basic，清空缓存冷启动，其中素材特征提取 3.40 秒）

**18. 第二次读取 cache 后耗时多少？** 6.20 秒（同一用例第二次运行，素材特征提取降到 0.31 秒）

**19. 最推荐的算法组合是什么？** D（见 BENCHMARK_REPORT.md 的横向对比）

**20. 哪些情况无法可靠反溯？**
- 推荐组合在本次全部用例上均正确定位；仍需注意下列已知限制。

## 已知限制

- 素材内容在时间上高度自相似（长时间静止、循环纹理）时，任何全局描述子都难以区分时间点，本工具会通过时序一致性与 margin 判据输出 UNKNOWN，而不是硬给一个来源。
- 变速幅度超出 `alignment.min_slope ~ max_slope`（默认 0.5x~2.0x）时不会被识别为同一片段。
- 极短片段（< `alignment.min_segment_sec`）信息量不足，置信度会被长度因子衰减。
- 画面被裁切会显著改变全局描述子：实测 10% 边缘裁切后，真值区间的相似度低于同素材其他时间点的伪匹配，直接对齐会建不出时序链。为此在片段被判为 UNKNOWN 时会自动做「裁边补偿」——把候选素材按 `refinement.crop_views`（默认 0.9 / 0.8）中心裁切到相同视野后重新对齐。代价是候选素材要按比例全片重提特征（结果进缓存），因此只对困难片段触发。裁切比例超出该列表时仍可能失败。
- TransVCL 困难样本判别为可选依赖，未安装时不参与流程（不影响主链路）。

